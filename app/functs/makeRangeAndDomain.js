/*

What I need this to do is take a list of text elements and then 
make a range or domain 

It needs to know information about the range, the targeted end points and the position of the text element it is linked to. 


*/


const makeRangeAndDomain = function(t, arr) {
  let args = [...arguments];

  return [0,1];

}

export default makeRangeAndDomain